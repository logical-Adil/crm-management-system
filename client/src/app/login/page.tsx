import { LoginScreen } from "./login-screen";

export const metadata = {
  title: "Sign in | CRM",
  description: "Sign in to your organization CRM",
};

export default function LoginPage() {
  return <LoginScreen />;
}
